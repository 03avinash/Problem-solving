// Print a table containing multiplication tables

/***
 * Step 1 - Create a function that takes an input "num"
 * Step 2 - Declare the result variable.
 * Step 3 - Run a loop upto 10.
 * Step 4 - Multiply input with every element and store the value in result variable.
 * Step 5 - Return result.
 */

// function tableOf(num) {
//   let result;
//   for (let i = 1; i <= 10; i++) {
//     result = num * i;
//     console.log(num + " * " + i + " = ", result);
//   }
// }
// tableOf(2);
